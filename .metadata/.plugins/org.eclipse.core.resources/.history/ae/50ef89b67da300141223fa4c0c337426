
public class chapter4a {
	int maxHeight(TreeNode tree){
		if(tree == null){
			return 0;
		}else{
			return Math.max(maxHeight(tree.left), maxHeight(tree.right))+1;
		}
	}
	
	Boolean isBalanced(TreeNode tree){
		int difference = maxHeight(tree.left)-maxHeight(tree.right);
	}
}

