
public class chapter2b {
	public int kthToLast(Node n){
		int length;
		Node forLength = n;
		Node findKth = n;
		while (forLength != null){
			forLength = forLength.next;
			length++;
		}
		for (int i=0; i<length; i++){
			findKth = findKth.next;
		}
		return findKth.data;
	}
}
